const Drawer = (props) => {
  const { showDrawer } = props;

  return (
    <div className={`Drawer ${showDrawer ? "Drawer-active" : ""} `}>
      jj
      {/* date 
    mes annonces
    mon profile 
    message 
    a propos
    suport
    theme
    contact
    se déconecter
    faire un don
    */}
    </div>
  );
};
export default Drawer;
